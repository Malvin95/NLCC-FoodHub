
export enum EngagementRequestType {
  HELP = 'help',
  VOLUNTEER = 'volunteer',
  DONATION = 'donation',
  QUESTION = 'question',
}

export enum EngagementRequestStatus {
  URGENT = 'urgent',
  OPEN = 'open',
  IN_PROGRESS = 'in-progress',
  RESOLVED = 'resolved',
}

export enum View {
  DASHBOARD = 'dashboard',
  ENGAGEMENT = 'engagement',
  INVENTORY = 'inventory',
  EVENTS = 'events',
  VOLUNTEERS = 'volunteers',
  HISTORY = 'history',
}

export enum StatusLevels {
  LOW = 'low',
  MEDIUM = 'medium',
  GOOD = 'good',
}